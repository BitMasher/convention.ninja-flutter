
import 'package:flutter/material.dart';

class InventoryLandingPage extends StatelessWidget {
  const InventoryLandingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SelectableText("inventory landing page");
  }

}
name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  cache:
    path: ../cache
  equatable: ^2.0.3
  firebase_auth: ^3.6.2
  firebase_core: ^1.20.0
  json_annotation: ^4.6.0
  http: ^0.13.4
  flutter:
    sdk: flutter
dev_dependencies: {build_runner: ^2.2.0, json_serializable: ^6.3.1}